{
  "name": "typescript-with-frameworks",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/hello.ts",
    "built": "tsc"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}